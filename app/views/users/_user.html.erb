<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !user.current_user?(current_user) %>
    | <%= link_to t("users.register.form.delete"), user, method: :delete,
      data: {confirm: t("users.confirm")} %>
  <% end %>
</li>
