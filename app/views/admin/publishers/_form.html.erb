<%= form_for [:admin, @publisher] do |f| %>
  <%= render "shared/error_messages", object: @publisher %>

  <%= f.label :name %>
  <%= f.text_field :name, class: "form-control" %>

  <br>
  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>
